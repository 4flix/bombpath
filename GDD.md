# BombPath 게임 기획서

## 1. 개요
- **장르**: 실시간 사다리타기 서바이벌 데스매치
- **플레이 인원**: 1인용(vs AI), 2인용(vs 실제 플레이어), 확장 시 다인 토너먼트
- **플랫폼**: 웹 브라우저 (PC/모바일 반응형)
- **핵심 한 줄 요약**: 고전 사다리타기 위에 숨겨진 5개의 폭탄을 피해 방어 아이템을 챙기며 상대보다 오래 살아남는 실시간 대전 게임.

## 2. 게임 콘셉트
전통적인 사다리타기(세로줄 + 랜덤 가로줄)를 실시간 대전 형식으로 재해석한다.
- 사다리 아래쪽 도착 지점(레인 끝) 중 **5곳에 폭탄**이 가려진 채 숨겨져 있다.
- 사다리 중간의 가로줄(연결선) 위에는 **랜덤한 위치에 방어 아이템 2개**가 숨겨져 있으며, 이를 밟고 지나가면 폭탄 1회를 방어할 수 있는 실드를 얻는다.
- 플레이어는 자신이 출발할 세로줄(레인)을 서로 겹치지 않게 선택한 뒤, 동시에 하강을 시작한다.
- 가로줄을 만나면 사다리 규칙에 따라 자동으로 옆 레인으로 이동하며, 최종적으로 도착한 지점이 폭탄이면 사망(실드가 있으면 실드 소모 후 생존), 아니면 생존한다.
- 생존한 플레이어는 다음 라운드(다음 상대)로 진출하고, 이를 반복하여 **최후의 1인이 남을 때까지** 게임이 계속된다.

## 3. 핵심 게임 루프
1. **매칭/로비**: 플레이어가 방을 만들거나 참가. 1vAI 또는 1v1 모드 선택.
2. **레인 선택 (Pick Phase)**: 제한 시간(예: 5초) 안에 서로 다른 세로줄 중 하나를 선택. 동시 공개.
3. **하강 (Descent Phase)**: 두 플레이어가 동시에 실시간으로 사다리를 타고 내려간다. 가로줄을 만나면 자동으로 옆줄로 이동(사다리 규칙). 진행 속도는 고정이며 실시간으로 상대의 위치도 함께 보인다(심리전 요소: 상대가 어디로 가는지 보임).
4. **결과 판정**: 도착 지점 공개 → 폭탄 여부 판정 → 생존/탈락 결정.
5. **라운드 진행**: 생존자는 다음 라운드로, 탈락자는 게임 종료(관전 모드 전환 가능).
6. **반복**: 최후의 1인이 남을 때까지 새로운 사다리(레인 수, 폭탄/아이템 위치 재생성)로 반복.

## 4. 상세 규칙

### 4.1 사다리 구조
- 세로줄(레인) 수: 기본 4~6개 (라운드가 진행될수록 난이도 조절 가능, 예: 레인 수 감소)
- 가로줄(연결선): 각 레인 사이에 랜덤 생성. 단, 동일 높이에서 인접한 두 칸이 동시에 연결되지 않도록 하여 사다리 규칙(교차 금지) 보장.
- 사다리 전체는 서버에서 시드 기반으로 생성되어 양쪽 클라이언트에 동일하게 전달됨 (공정성 보장).

### 4.2 폭탄 배치
- 하단 도착 지점 중 **5곳**에 폭탄 배치 (레인 수가 5곳보다 적을 경우 최소 1곳은 안전지대 보장하도록 레인 수 하한 설정, 예: 레인 6개 중 5개 폭탄 + 1개 안전).
- 폭탄 위치는 라운드 시작 시 서버에서 랜덤 생성, 게임 종료(도착) 시점까지 양쪽 모두에게 비공개.

### 4.3 방어 아이템 (실드)
- 사다리의 가로줄(연결선) 위 랜덤한 2곳에 배치.
- 하강 중 해당 가로줄을 통과하면 자동 획득.
- 실드 보유 시 폭탄을 밟아도 1회 방어(실드 소모, 생존). 실드는 최대 1개까지 중첩 없이 보유(2개 다 먹으면 2회 방어 가능하도록 확장 가능).
- 아이템 표시는 하강 중 실시간으로 보이되, 상대방이 어떤 아이템을 먹었는지도 서로 확인 가능(심리전+정보전 요소).

### 4.4 승패 판정
- 하강 종료 후 도착 지점이 폭탄이고 실드가 없으면 **탈락(사망)**.
- 폭탄이 아니거나 실드로 방어했으면 **생존**.
- 1v1 모드: 한쪽이 탈락하면 즉시 상대 승리. 둘 다 생존하거나 둘 다 탈락하면 무승부 처리 후 서든데스(사다리 재생성, 폭탄 수 증가) 진행.
- 1vAI 모드: AI는 서버 로직상 레인 선택 시 약한 확률적 휴리스틱(완전 랜덤 또는 약간의 안전 회피 로직)으로 결정하여 난이도 조절 가능.

### 4.5 토너먼트 진행 (멀티 라운드)
- 최후의 1인이 남을 때까지 라운드를 반복.
- 다인 참가 시(확장 기능) 실제 "사다리타기 대진표"처럼 토너먼트 브래킷을 구성하여 승자끼리 다음 라운드에서 매칭.
- 매 라운드마다 사다리 구조/폭탄/아이템 위치는 새로 생성됨 (동일 패턴 암기 방지).

## 5. 모드
| 모드 | 설명 |
|---|---|
| 1 vs AI | 싱글 플레이. 난이도(쉬움/보통/어려움)에 따라 AI의 레인 선택 로직 차등 |
| 1 vs 1 (실시간) | 방 생성/코드 참가 방식으로 실제 유저와 실시간 대전 |
| (확장) 토너먼트 | 4/8인 등 다수가 참여, 사다리 대진표로 순차 대결하여 최종 챔피언 결정 |

## 6. 조작 및 UI
- **레인 선택**: 클릭/탭으로 세로줄 선택, 제한시간 내 미선택 시 랜덤 자동 배정.
- **하강 연출**: 캐릭터(또는 마커)가 실시간으로 사다리를 따라 내려가는 애니메이션. 상대 마커도 동시에 표시.
- **결과 연출**: 도착 지점에서 폭탄 폭발 이펙트 / 안전 도착 이펙트 / 실드 방어 이펙트.
- **로비 UI**: 방 생성/참가 코드, 대기 중 상대 표시, 라운드 수 및 전적 표시.
- **관전 모드**: 탈락 후에도 남은 경기를 관전할 수 있도록 지원(확장).

## 7. 기술 아키텍처
- **백엔드**: Node.js + Express + Socket.io — 방/로비 관리, 사다리·폭탄·아이템 시드 생성, 실시간 위치 동기화(권위 서버 방식으로 부정행위 방지), 판정 로직.
- **프론트엔드**: Vanilla JS + HTML5 Canvas — 별도 빌드 없이 정적 파일로 서빙, 사다리 렌더링 및 애니메이션.
- **통신**: Socket.io 이벤트 기반 (방 생성/참가, 레인 선택, 하강 시작/진행/도착, 라운드 결과, 게임 종료).
- **배포**: GitHub 저장소 + Render.com Web Service (Node 런타임, 단일 서비스로 정적 파일과 소켓 서버 동시 서빙).

## 8. 향후 확장 아이디어
- 폭탄 개수/레인 수를 라운드마다 점진적으로 어렵게 조정
- 아이템 종류 다양화 (예: 상대 방해 아이템, 투시 아이템)
- 랭킹/전적 저장 (DB 연동)
- 다인 토너먼트 브래킷 모드 정식 지원
